# Test Environment Configuration
NODE_ENV=test

# Test Database
# IMPORTANT: Uses a separate test database to avoid corrupting development data
DATABASE_URL=postgresql://localhost:5432/coursesignal_test

# Redis (optional for tests)
REDIS_URL=redis://localhost:6379/1

# JWT Secrets (test values)
JWT_SECRET=test-jwt-secret-key-for-testing
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Email (mock or test service)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=test-user
SMTP_PASSWORD=test-password
FROM_EMAIL=test@coursesignal.test

# Stripe (test keys)
STRIPE_SECRET_KEY=sk_test_test_key_for_testing
STRIPE_WEBHOOK_SECRET=whsec_test_webhook_secret
STRIPE_PRICE_ID=price_test_price_id

# Kajabi (mock credentials)
KAJABI_CLIENT_ID=test-kajabi-client-id
KAJABI_CLIENT_SECRET=test-kajabi-client-secret
KAJABI_REDIRECT_URI=http://localhost:3002/api/kajabi/callback

# Teachable (mock credentials)
TEACHABLE_CLIENT_ID=test-teachable-client-id
TEACHABLE_CLIENT_SECRET=test-teachable-client-secret
TEACHABLE_REDIRECT_URI=http://localhost:3002/api/teachable/callback

# Encryption (32-character key for testing)
ENCRYPTION_KEY=test-encryption-key-32-chars!!

# App Configuration
APP_URL=http://localhost:5173
PORT=3002

# Test-specific settings
TEST_TIMEOUT=10000
